<form method="post" action="{{ path('desa_delete', {'id': desa.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ desa.id) }}">
    <button class="btn">Delete</button>
</form>
